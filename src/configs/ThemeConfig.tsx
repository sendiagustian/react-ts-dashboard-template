import createTheme from "@mui/material/styles/createTheme";

export let theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#f5f5f5",
        },
        secondary: {
            main: "#edf2ff",
        },
    },
});
